# Abril Cuenca Art

Portafolio visual realizado con React y Vite.

## Desarrollo local

1. Instalar dependencias con `npm install`
2. Iniciar el entorno local con `npm run dev`
3. Abrir la URL que muestre Vite

## Producción

```bash
npm run build
```

La carpeta lista para publicar es `dist/`.
